# Evals

`run_accuracy.py` is a minimal live-model example, not a sufficient production suite.
Add versioned datasets for output accuracy, expected tool calls, Memory behavior, RAG
grounding, user isolation, latency, and cost. Run small deterministic gates on each PR
and a larger real-provider suite before release.
